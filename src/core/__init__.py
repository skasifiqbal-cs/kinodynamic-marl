"""Shared building blocks: robots, environments, observations, collision,
conflict predicates, reward shaping, networks, initializers, rendering.

Both ``src.planning`` and ``src.rl`` import from here; nothing here imports
from either of them.
"""
