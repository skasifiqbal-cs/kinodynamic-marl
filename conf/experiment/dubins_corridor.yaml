# @package _global_
# Run: python train.py +experiment=dubins_corridor
defaults:
  - override /env: corridor
  - override /shaping: dubins
  - override /network: mlp
  - override /obs: full_state
  - override /init: random_heading

train:
  timesteps: 2_000_000
  learning_rate: 1.0e-4
