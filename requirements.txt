# Core runtime dependencies.
torch>=2.0.0
hydra-core>=1.3.0
omegaconf>=2.3.0
pettingzoo>=1.24.0
gymnasium>=0.29.0
skrl>=1.3.0,<2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
Pillow>=10.0.0

# Optional — NOT required. The Dubins potential falls back to the bundled pure-Python
# implementation (src/shaping/_dubins_py.py) when the `dubins` C extension is absent.
#   dubins>=1.0.1        # needs gcc: sudo apt install python3-dev build-essential
# Optional — experiment tracking (disabled by default; wandb.enabled=false).
#   wandb>=0.17.0
# Optional — interactive checkpoint viewer (scripts/viewer.py).
#   streamlit>=1.30.0
